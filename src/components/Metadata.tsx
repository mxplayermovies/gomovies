export interface Metadata {
  title: string;
  description: string;
  ogImage?: string;
  url: string;
  keywords: string;
  viewportSettings?: string;
  robots?: string;
  canonicalUrl?: string;
}

export default Metadata;
